spring:
  application:
    name: ecommerce

  #spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ecom_code_with_mithun
    username: root
    password: admin

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

server:
  port: 8081

app:
  client:
    url: http://localhost:4200/login
